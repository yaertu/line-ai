[package]
name = "line-cli"
version = "0.1.0"
description = "SmoothUI tabanlı yerel OpenAI ve Gemini masaüstü istemcisi"
authors = ["yaaertu"]
edition = "2021"
default-run = "line-cli"

[lib]
name = "line_cli_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
